package main

import (
	"testing"
)

func TestGetHash(t *testing.T) {
	/*for i := 0; i < 13; i++ {
		code, hash, err := generateStringAndHash()
		if err != nil {
			i--
			continue
		}
		t.Logf(", '%s'), -- code: %s\n", hash, code)
	}*/
}
